services:
  - type: web
    name: telegram-helper-bot
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py
    envVars:
      - key: BOT_TOKEN
        sync: false
      - key: NVIDIA_API_KEY
        sync: false
